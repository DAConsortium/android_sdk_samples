<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.co.dac.sdk.ma.sample">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- for testing -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".adpod.AdpodActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".vertical.VerticalActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".content.ContentActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".no_content.NoContentActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".vmap.VmapActivity"
            android:label="@string/app_name" />
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="dc022b48e5f901e78b196997bf70773dd20f990b" />
    </application>

</manifest>
